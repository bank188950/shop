# แนวทางการทำงานใน Shop workspace

## ขอบเขตและแหล่งอ้างอิงหลัก

- Workspace นี้ประกอบด้วย `frontend/`, `backend/` และ `specs/` โดย `specs/` เก็บเอกสารออกแบบและ `specs/database/schema.sql`
- อ่านไฟล์นี้ก่อน จากนั้นอ่านไฟล์คำแนะนำที่อยู่ใกล้กับโค้ดที่จะเปลี่ยนมากที่สุดก่อนแก้ไข
- ห้ามแก้ไข `CLAUDE.md` เพราะเนื้อหาซ้ำกับที่ `AGENTS.md` อ้างอิงไว้แล้ว ให้ `AGENTS.md` เป็นแหล่งอ้างอิงหลักเพียงไฟล์เดียว หากต้องปรับแนวทางการทำงาน ให้แก้ที่ `AGENTS.md`
- งาน UI สำหรับเส้นทางลูกค้าต้องอ้างอิง `specs/design-user.md` และเส้นทางผู้ดูแลระบบต้องอ้างอิง `specs/design-admin.md` หากโค้ดขัดกับเอกสารออกแบบ ให้ปรับเอกสารในการเปลี่ยนแปลงเดียวกัน หรือขอคำแนะนำก่อนดำเนินการ

## Frontend

- คงสถาปัตยกรรม React + Vite + TypeScript ไว้ใน `frontend/` ห้ามย้าย framework เพื่อทำฟีเจอร์
- ใช้ `lucide-react` ซ้ำสำหรับไอคอน UI ห้ามเพิ่มปุ่มที่มีเฉพาะไอคอนโดยไม่มี accessible label เว้นแต่มีป้ายกำกับเพื่อการเข้าถึงแล้ว
- ห้ามใช้ `!important` ใน CSS; แก้ลำดับการประกาศหรือ specificity ของ selector ให้ถูกต้องแทน เช่น เติมชื่อ element หน้า class เพื่อให้ specificity เท่ากับกฎที่ชนกัน ยกเว้น 2 กรณี คือ (1) การบังคับปิด animation ใน `prefers-reduced-motion` เพื่อ accessibility และ (2) การ override style ภายในของ third-party component ที่ไม่เปิด prop ให้ปรับ; สำหรับ component ของ `shadcn/ui` ให้ส่ง class ผ่าน prop `className` ก่อนเสมอ เพราะ `cn()` จะตัด class ที่ขัดแย้งกันออกให้
- ฟอร์มกรอกข้อมูลในส่วนผู้ดูแลระบบ (back office) ให้ใช้ component ของ `shadcn/ui` เช่น `Input`, `Select` และ `Textarea` เป็นค่าเริ่มต้น; ใช้ HTML native เฉพาะเมื่อ component ไม่รองรับความต้องการนั้นจริง
- การตรวจสอบฟอร์มให้เริ่มจากกฎพื้นฐานเท่านั้น ได้แก่ ค่าว่าง, รูปแบบอีเมล และค่าตัวเลข; เพิ่มกฎเฉพาะหรือซับซ้อนเมื่อผู้ใช้ระบุความต้องการชัดเจน
- ฟอร์มที่ส่งข้อมูลเพื่อเข้าสู่ระบบ, สร้าง, แก้ไข หรือบันทึกข้อมูล ต้องใช้ `react-hook-form` ร่วมกับ `zod` และ `zodResolver` จาก `@hookform/resolvers`; ให้กำหนด schema ไว้ใกล้กับฟีเจอร์นั้น และห้ามเขียน validation ซ้ำใน `onSubmit`
- เลือกตรวจตามชนิดของ UI: `Input`/`Textarea` ที่เป็นข้อมูลบังคับตรวจค่าว่าง, `Input type="email"` ตรวจรูปแบบอีเมล, `Input` ตัวเลขตรวจว่าเป็นตัวเลข, `Select`/radio ตรวจว่าเลือกค่าแล้ว, และช่องอัปโหลดตรวจชนิดกับขนาดไฟล์ตามข้อความที่แสดงใน UI; ไม่เพิ่มกฎธุรกิจ เช่น ความยาวสูงสุด, รูปแบบเฉพาะ, หรือการเทียบค่าข้ามช่อง จนกว่าผู้ใช้จะสั่ง
- ไม่ต้องใช้ form validation กับ UI ที่ไม่ใช่การบันทึกข้อมูล เช่น search, filter, pagination, ปุ่มสลับสถานะ, ปุ่มเพิ่ม/ลดจำนวน หรือกล่องพิมพ์แชต; ให้ตรวจค่าเฉพาะจุดเท่าที่จำเป็นต่อการกดใช้งาน
- แสดงข้อความผิดพลาดภาษาไทยใต้ UI ที่ผิด, ใช้ `aria-invalid` และเชื่อม `aria-describedby`; ข้อความต้องไม่ทำให้เลย์เอาต์ของฟอร์มกระโดด
- ทำให้หน้าลูกค้าตอบสนองตั้งแต่ความกว้าง 320px ขึ้นไป รักษาสถานะ keyboard focus, ขนาดพื้นที่กดอย่างน้อย 44px เมื่อเหมาะสม และใช้ข้อความภาษาไทยที่อ่านง่าย
- ตารางหรือรายการที่มี pagination ให้ใช้ 10 รายการต่อหน้าเป็นค่าเริ่มต้นเสมอ จนกว่าผู้ใช้จะสั่งเป็นจำนวนอื่นสำหรับหน้านั้นโดยเฉพาะ
- สถานะ UI ต้องทำงานจริง ปุ่มเพิ่มสินค้าต้องอัปเดตตะกร้า และตัวเลือกช่วงส่ง/หมวดหมู่ต้องแสดงสถานะที่เลือก
- วางฟีเจอร์ผู้ใช้ใน `frontend/src/features/user/`, ฟีเจอร์ผู้ดูแลใน `frontend/src/features/admin/`, layout ที่ใช้ร่วมกันใน `frontend/src/layouts/` และ visual token ส่วนกลางใน `frontend/src/styles/index.css`
- จัดโค้ดของแต่ละเรื่องไว้ใน `frontend/src/features/{user|admin}/{feature}/`; เก็บ component, `types.ts`, `schema.ts`, `hooks/` และ `utils/` ที่ใช้เฉพาะเรื่องนั้นไว้ด้วยกัน และไม่สร้างไฟล์หรือโฟลเดอร์ว่างเผื่อไว้; ย้ายเฉพาะ hook หรือ utility ที่ใช้ร่วมกันจริงไป `src/hooks/` หรือ `src/utils/`
- เก็บข้อมูล mock ที่ใช้รอการเชื่อมฐานข้อมูลไว้ใน `frontend/src/data/user/` หรือ `frontend/src/data/admin/` ตามฝั่งที่ใช้ เพื่อให้ลบหรือแทนที่ด้วย API ได้ชัดเจน
- `frontend/src/hooks/`, `frontend/src/utils/` และ `frontend/src/lib/` ให้วางเป็นไฟล์ตรง ๆ ในโฟลเดอร์นั้น ห้ามแยกโฟลเดอร์ย่อย (เช่น `user/`, `admin/`) เว้นแต่แจ้งผู้ใช้ก่อน; หากยังไม่มีไฟล์ที่ใช้ร่วมกันจริง ไม่ต้องสร้างโฟลเดอร์ทิ้งไว้ว่าง ๆ
- แยก `src/utils/` กับ `src/lib/` ตามความซับซ้อน: `utils/` คือฟังก์ชันจบในตัว รับค่าเข้า-คืนค่าออก ใช้ทอดเดียว เช่น แปลงวันที่ แปลงหน่วย ตัดคำ; `lib/` คือโมดูลที่มีการตั้งค่าหรือสถานะและถูกเรียกใช้ต่อหลายทอด เช่น axios instance
- โฟลเดอร์ระดับบนใน `frontend/src/` ถือว่าครบแล้ว ได้แก่ `api/`, `app/`, `components/`, `data/`, `features/`, `hooks/`, `layouts/`, `lib/`, `pages/`, `stores/`, `styles/`, `utils/` (โดย `hooks/` จะสร้างเมื่อมี hook ที่ใช้ร่วมกันจริงเท่านั้น) ให้เพิ่มโค้ดใหม่ลงในโฟลเดอร์ที่มีอยู่เสมอ ห้ามสร้างโฟลเดอร์ระดับบนเพิ่ม เว้นแต่ผู้ใช้สั่งชัดเจน
- รวม CSS ไว้ 2 ไฟล์เท่านั้น คือ `frontend/src/styles/index.css` สำหรับ visual token ส่วนกลางและสไตล์ฝั่งลูกค้า และ `frontend/src/styles/index-admin.css` สำหรับสไตล์ฝั่งผู้ดูแล; ทั้งสองไฟล์ถูก import รวมกันทั้งแอปที่ `src/main.tsx` การแยกไฟล์จึงเป็นการจัดระเบียบ ไม่ได้แยก scope ต้องตั้งชื่อ class ไม่ให้ชนกันข้ามฝั่ง; ห้ามแยกไฟล์ CSS เพิ่ม เว้นแต่ผู้ใช้สั่งชัดเจน
- ก่อนส่งมอบการเปลี่ยนแปลง frontend ให้รัน `npm run build` จาก `frontend/` และแก้ compilation error ที่เกิดขึ้นจริง

## Backend และฐานข้อมูล

- เก็บความลับไว้ใน `backend/.env` ห้าม commit หรือเปิดเผย credential ในโค้ดฝั่ง client
- `specs/database/schema.sql` เป็นแหล่งอ้างอิงโครงสร้างฐานข้อมูลเพียงไฟล์เดียว โปรเจกต์นี้ไม่ใช้โฟลเดอร์ migration; เมื่อเปลี่ยนโครงสร้าง ให้แก้ `schema.sql` ให้ตรงกับผลลัพธ์สุดท้าย แล้วแจ้งคำสั่ง `ALTER TABLE` ที่ต้องรันกับฐานข้อมูลที่มีอยู่ให้ผู้ใช้ในคำตอบเดียวกัน และระบุ API contract ให้ชัดเจนในโค้ด backend
- โปรเจกต์นี้ไม่ใช้ soft delete: การลบข้อมูลที่ผู้ใช้ยืนยันแล้วให้ลบออกจากฐานข้อมูลจริง และห้ามเพิ่มฟิลด์หรือเงื่อนไข `deleted_at` / `deleted_by` เพื่อจำลองการลบ
- ห้ามเปลี่ยนข้อมูล production, พฤติกรรมการชำระเงิน หรือข้อกำหนดการยืนยันตัวตนโดยไม่ได้รับอนุมัติอย่างชัดเจน

## ข้อควรระวัง

- คำอธิบายให้ใช้ภาษาไทยเป็นหลัก ยกเว้นคำสั่งและคำทับศัพท์ภาษาอังกฤษที่จำเป็น
- การแสดงราคาให้ใช้หน่วย `บาท` หลังตัวเลข เช่น `60 บาท` ห้ามใช้สัญลักษณ์ `฿`
- ห้ามเพิ่มหัวข้อระดับ `#` หรือ `##` ให้ใช้หัวข้อเดิมก่อนเสมอ หากจำเป็นต้องเพิ่มจริง เพิ่มได้แต่ต้องแจ้งผู้ใช้ว่าเพิ่มหัวข้ออะไรและเพราะอะไร; ข้อนี้ไม่ใช้กับหัวข้อที่ automation ของ OpenWiki สร้างในบล็อก `OPENWIKI:START/END`
- เมื่อมีการอัปเดตไฟล์ในโฟลเดอร์ `openwiki/` ให้คงเนื้อหาทั้งหมดเป็นภาษาไทยเสมอ (รวมถึงหัวข้อ, ย่อหน้า, ตาราง, ฟิลด์ `title`/`description` ใน frontmatter และข้อความท้ายลิงก์ในไฟล์ `index.md`) โดยคงภาษาอังกฤษไว้เฉพาะสิ่งที่จำเป็นต้องทับศัพท์ ได้แก่ โค้ด/คำสั่ง, ชื่อไฟล์และ path, ชื่อ table/คอลัมน์/ฟิลด์, ค่าสถานะในโค้ด, ชื่อเทคโนโลยี (React, Vite, PHP, MySQL, API ฯลฯ), ฟิลด์ `tags` และ mermaid diagram หากแปลข้อความหัวข้อเป็นไทย ต้องอัปเดตลิงก์ที่อ้างอิง anchor ของหัวข้อนั้นให้ตรงกันด้วย ข้อนี้เป็นฉบับอ้างอิงหลักของกฎภาษาไทยใน `openwiki/` หากต้องแก้กฎให้แก้ที่นี่เท่านั้น ห้ามแก้สำเนาที่อยู่ในบล็อก `OPENWIKI:START/END` เพราะ automation เขียนทับได้

## มาตรฐานคุณภาพ

- เลือก patch ขนาดเล็กที่มีขอบเขตสอดคล้องกัน แทนการเก็บกวาดโค้ดที่ไม่เกี่ยวข้อง
- เขียน JavaScript, TypeScript และ PHP ให้เรียบง่าย อ่านง่าย และแยกไฟล์เท่าที่จำเป็นต่อการดูแลรักษาเท่านั้น; หลีกเลี่ยงการสร้าง abstraction, class หรือโครงสร้างไฟล์หลายชั้นเมื่อ function ตรงไปตรงมาเพียงพอ
- เมื่อเพิ่มโค้ดเรียก API ใน frontend ให้แยกตามฝั่ง `src/api/user/` และ `src/api/admin/` แม้เป็น resource เดียวกัน เพื่อให้ไล่ request, สิทธิ์ และ bug ได้ง่าย; เก็บเฉพาะ helper ที่ใช้ร่วมกันจริงไว้ใน `src/lib/` หรือ `src/utils/`
- รักษาการแก้ไขที่ผู้ใช้ทำไว้ใน worktree ที่มีการเปลี่ยนแปลง
- ตรวจสอบเฉพาะส่วนที่ได้รับผลจากการเปลี่ยนแปลง และรายงานพฤติกรรมที่ยังไม่ได้ตรวจสอบให้ชัดเจน

<!-- OPENWIKI:START -->

## OpenWiki

This repository uses OpenWiki for recurring code documentation. Start with `openwiki/quickstart.md`, then follow its links to architecture, workflows, domain concepts, operations, integrations, testing guidance, and source maps.

The scheduled OpenWiki GitHub Actions workflow refreshes the repository wiki. Do not hand-edit generated OpenWiki pages unless explicitly asked; prefer updating source code/docs and letting OpenWiki regenerate.

<!-- OPENWIKI:END -->
